#include <stdio.h>
#include <stdlib.h>

int main (int argc, char * const argv [] {
  int l = 1;
  int i = 1;
  
  while ( ( (l*i)/l ) == i) {
    l = l*i;
    i++;
 }
 i--;
 printf ("valeur de i = %d, valeur de %d! = %lu, %lu, %lu \n", i, i, l, f(i));
 
 return EXIT_SUCCESS;
 
 }
